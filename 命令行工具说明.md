# NeuVulkanRender 命令行调试工具

渲染器内置了基于终端 (stdin) 的命令行调试系统 `Console`，用于加载工程/场景、修改渲染参数、控制相机、查询节点状态、截图等，便于自动化测试与 RenderDoc 抓帧分析。

## 使用方式

程序启动后，直接在运行它的终端窗口输入命令即可（支持实时交互输入）。

也支持管道/重定向批量注入命令（脚本化）：

```bash
# Windows cmd
type commands.txt | .\build\NeuVulkanRender.exe

# PowerShell
Get-Content commands.txt | .\build\NeuVulkanRender.exe

# 启动时直接加载工程（目录或 project.json）
.\build\NeuVulkanRender.exe "D:/desktop/items/prog002"
```

命令在渲染线程上执行，每帧最多处理一条，天然线程安全。路径含空格时用双引号包裹：`load project "D:/My Projects/A"`。

## 命令一览

在程序内输入 `help` 可实时查看所有命令。

### 工程 / 场景

| 命令 | 说明 |
|---|---|
| `load project <path>` | 加载工程（目录或 project.json 路径），自动加载全部场景并恢复相机 |
| `load scene <path>` | 直接加载单个场景文件（不依赖工程） |
| `save` | 保存当前工程 |
| `load-project <path>` | `load project` 的快捷方式 |
| `load-scene <path>` | `load scene` 的快捷方式 |

### 渲染参数

| 命令 | 说明 |
|---|---|
| `set vsync <on\|off>` | 垂直同步开关 |
| `set fps <n>` | 目标帧率（0 = 不限） |
| `set taa <on\|off>` | TAA 开关 |
| `set sr <scale>` | 超分辨率缩放（1.0 = 原生，2.0 = 半分辨率渲染） |
| `set shadow-res <n>` | 阴影贴图分辨率（如 2048） |
| `set debug-mode <0-10>` | 调试视图：0=Shaded 1=Wireframe 2=Albedo 3=Normal 4=Depth 5=Smoothness 6=Specular 7=Occlusion 8=MaterialFlags 9=ShadingID 10=Emission |
| `set bloom <on\|off>` | 泛光开关 |
| `set ssao <on\|off>` | SSAO 开关 |
| `set ssr <on\|off>` | SSR 开关 |
| `set tonemap <on\|off>` | 色调映射开关 |
| `set gamma <on\|off>` | Gamma 校正开关 |
| `set light-dir <x> <y> <z>` | 方向光方向 |
| `set skybox-brightness <v>` | 天空盒亮度 |
| `set camera-control <on\|off>` | 编辑器相机控制开关 |

### 相机

| 命令 | 说明 |
|---|---|
| `camera pos <x> <y> <z>` | 设置相机位置 |
| `camera yawpitch <yaw> <pitch>` | 设置相机朝向 |
| `camera reset` | 重置相机 |

### 场景节点

| 命令 | 说明 |
|---|---|
| `nodes` | 列出当前场景全部节点（层级、类型、位置、激活状态） |
| `node info <name>` | 查看节点变换信息 |
| `node move <name> <x> <y> <z>` | 设置节点位置 |
| `node rotate <name> <x> <y> <z>` | 设置节点旋转（欧拉角，度） |
| `node scale <name> <x> <y> <z>` | 设置节点缩放 |

### 截图 / 其他

| 命令 | 说明 |
|---|---|
| `screenshot [path]` | 保存截图，默认 `console_screenshot.png`（在帧末安全执行，每帧最多一张） |
| `sleep <ms>` | 暂停指定毫秒（用于脚本时序控制，等待场景加载/渲染） |
| `info` | 显示渲染器/后处理/相机/工程场景状态 |
| `echo <text>` | 输出文本 |
| `quit` / `exit` | 退出程序 |

## 示例脚本

```text
# 加载工程，等场景渲染稳定后截图
load project "D:/desktop/items/prog002"
sleep 5000
info
nodes
set taa off
set debug-mode 2
screenshot albedo_view.png
sleep 500
set debug-mode 0
camera pos 0 2 6
screenshot shaded_view.png
sleep 500
quit
```

## RenderDoc 抓帧配合

1. 启动应用并加载目标工程（命令行参数或 `load project` 均可）
2. 用 RenderDoc 注入抓帧即可分析，命令执行后的状态（参数/相机/场景）即为抓帧时的状态
3. 验证画面可用 `screenshot` 命令在 RenderDoc 外快速确认内容

## 架构说明

- `Console::Init()`：启动 stdin 监听线程，注册命令
- `Console::Update()`：主循环每帧调用，从队列取一条命令执行
- `Console::Shutdown()`：程序退出时停止线程
- 新增命令只需调用 `Console::RegisterCommand(name, usage, description, handler)` 注册一个 `bool(const std::vector<std::string>&)` 处理器
- 工程加载复用 `EditorGUI::LoadProjectFromPath` / `LoadSceneFromPath`，截图走 `RenderCore::RequestScreenshot`（帧末队列，避免与帧循环竞争）
